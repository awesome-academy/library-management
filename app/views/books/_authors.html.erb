<div class="widget brands mb-50">
  <p class="widget-title2 mb-30"><%= t "client.author" %></p>
  <div class="widget-desc">
    <ul>
      <% @authors.each do |author| %>
      <li>
        <%= link_to author.name, author_path(author.id) %>
      </li>
        <% end %>
    </ul>
  </div>
</div>
