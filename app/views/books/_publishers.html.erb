<div class="widget brands mb-50">
  <p class="widget-title2 mb-30"><%= t "client.publishers" %></p>
  <div class="widget-desc">
    <ul>
      <% @publishers.each do |publisher| %>
        <li>
          <%= link_to publisher.name, publisher_path(publisher.id) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>


